# 2026-05-17 整体测试报告

> 状态：通过。
>
> 本报告记录本轮“学生目标多周期、老师课堂讲评模板、管理端服务端筛选方案与批量审计、整体测试报告”集成后的全量验证结果。报告不记录 Cookie、API Key、密码或明文环境变量。

## 1. 测试范围

- 学生端：注册登录、已发布题练习、错后学习导航、补救路径、成长奖励、学习报告、核心素养目标多周期、下周期重开、推荐目标和目标历史筛选。
- 审核端：整卷拆题、审核筛选、低置信度处理、AI 重析、审核历史和一审发布边界。
- 老师端：班级报告、下钻、导出、复盘任务、提醒、复测反馈、二次任务、老师备注、课堂讲评模板和 CSV/Markdown 素材导出。
- 管理端：题库维护、知识点挂接、核心素养标签、服务端筛选方案、批量预览、二次确认、批次审计和审计检索。
- 业务红线：学生端只使用 `published` 题目，AI 结果必须审核，学生端不展示负面标签，API Key 不进入前端或报告。

## 2. 测试环境

| 项目 | 当前记录 |
| --- | --- |
| 测试日期 | 2026-05-17 |
| 测试执行人 | 主代理 + 子代理协作 |
| 操作系统 | 本机 macOS 环境 |
| Node.js 版本 | v25.9.0 |
| npm 版本 | 11.12.1 |
| 数据库 | Docker PostgreSQL 16，`chem2exam-postgres`，端口 5432 |
| 应用模式 | Next.js production build，`next start -p 4174` |
| 环境变量 | 已使用本地 `.env`；报告未记录任何明文敏感值 |

## 3. 代码与数据库状态

| 项目 | 结果 |
| --- | --- |
| 工作区 | 项目目录位于 `/Users/keikei/Documents/chem2exam`；Git 根目录在用户目录，项目内无独立 `.git` |
| 数据库服务 | PostgreSQL 容器在线 |
| Prisma Client | 已生成 |
| 数据表结构 | 已同步到当前 schema |
| 种子数据 | 已导入，包含已发布题和待审核候选题 |
| 注意事项 | `db:push` 首次因新增学生目标周期唯一键出现 Prisma 数据保护提示；使用本地测试库确认参数完成同步 |

## 4. 命令矩阵

| 命令 | 最终状态 | 备注 |
| --- | --- | --- |
| `npm run db:generate` | 通过 | Prisma Client 生成成功 |
| `npm run db:push` | 通过 | 初次提示新增唯一键可能影响旧数据；使用 `npx prisma db push --accept-data-loss` 同步本地测试库 |
| `npm run db:seed` | 通过 | Seed completed |
| `npm run typecheck` | 通过 | TypeScript 无错误 |
| `npm test` | 通过 | 19 条业务红线测试全部通过 |
| `npm run build` | 通过 | Next.js 构建成功，38 个页面/路由生成完成 |
| `npm run verify:db-flow` | 通过 | 学生注册、作答、补救、奖励、报告和排行榜通过 |
| `npm run verify:review-flow` | 通过 | 整卷拆题、低置信度、AI 重析、审核历史、一审发布通过 |
| `npm run verify:teacher-flow` | 通过 | 班级报告、复盘任务、核心素养目标、课堂讲评模板导出通过 |
| `npm run verify:admin-flow` | 通过 | 服务端筛选方案、批量预览确认、批次审计检索通过 |

## 5. 核心业务红线确认

| 红线 | 最终状态 | 证据 / 备注 |
| --- | --- | --- |
| 学生端只使用已审核并发布的题目 | 通过 | 单元测试、学生题目接口、老师复测入口和管理端验证均覆盖 |
| AI 输出必须进入人工审核，不得直接发布 | 通过 | `verify:review-flow` 覆盖 AI 候选题进入审核和一审发布 |
| 答错后必须提供错后学习导航 | 通过 | `verify:db-flow` 覆盖错题触发补救路径 |
| 奖励机制奖励成长行为 | 通过 | 单元测试和老师端验证覆盖复盘、突破、核心素养进步奖励 |
| API Key 只能保存在后端加密配置中 | 通过 | 单元测试覆盖加密存储和前端掩码展示；报告未记录明文 |
| 学生端不得展示负面标签 | 通过 | 单元测试和文本扫描覆盖；内部 `possible_guessing` 不展示给学生 |
| 题目保留来源、审核状态和发布状态 | 通过 | Prisma schema、管理端维护和审核流验证覆盖 |
| 知识图谱支持前置依赖关系 | 通过 | 种子数据和错后补救推荐覆盖 prerequisite |
| 人工审核结果优先级高于 AI 结果 | 通过 | 管理端维护写入 human 来源和审计记录 |
| 批量维护不能绕过一审发布边界 | 通过 | `verify:admin-flow` 覆盖批量发布拒绝和预览确认 |

## 6. 失败记录与处理

| 编号 | 场景 | 失败现象 | 处理结果 |
| --- | --- | --- | --- |
| F-001 | `npm run db:push` | Prisma 提示新增 `StudentLearningGoal` 周期唯一键可能影响旧数据，拒绝直接同步 | 确认为本地测试库 schema 演进提示，使用确认参数完成同步 |
| F-002 | `npm run verify:db-flow` | 本地历史测试数据较多，新学生未进入默认前 10 排行榜 | 为排行榜接口增加安全 `limit` 参数，验证脚本使用 `limit=100` |
| F-003 | `npm run verify:admin-flow` | 创建服务端筛选方案时 raw SQL 未写入非空 `updatedAt` | 补齐 `createdAt` / `updatedAt` 后复测通过 |

## 7. MVP 验收结论

| MVP 验收项 | 当前判断 |
| --- | --- |
| 学生“真题式诊断 -> 知识断点 -> 错后导航 -> 前置补救 -> 回到原知识点复测 -> 学习报告”闭环 | 通过 |
| 学生端只展示已发布题目，未发布题不进入练习 | 通过 |
| 答错后反馈温和且有行动建议 | 通过 |
| 成长奖励强调补救、复盘、突破和核心素养进步 | 通过 |
| 学习报告包含薄弱知识点、补救记录、下一步建议和核心素养目标 | 通过 |
| 审核和 AI 流程不绕过人工发布 | 通过 |
| 老师端和管理端扩展能力未破坏 MVP 红线 | 通过 |

整体结论：当前 MVP 和本轮扩展功能通过全量本地验证，可继续进入下一轮功能开发或人工浏览器抽查。
